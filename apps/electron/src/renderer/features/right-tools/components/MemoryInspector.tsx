import { ToolPanel } from "./ToolPanel";

export function MemoryInspector() {
  return (
    <ToolPanel title="Memory Inspector">
      <p className="muted">Pinned facts and summaries are reserved in the native schema for the next iteration.</p>
    </ToolPanel>
  );
}
